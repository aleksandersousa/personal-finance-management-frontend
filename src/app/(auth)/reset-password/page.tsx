import { ResetPasswordPage } from '@/presentation/pages';

export default function ResetPasswordPageRoute() {
  return <ResetPasswordPage />;
}

export const metadata = {
  title: 'Redefinir senha | Gestão Financeira',
  description: 'Redefina sua senha usando o token de redefinição',
};
